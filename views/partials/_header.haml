%header#header
	%h1
		%a{ :href => "/" }= site_title
	%nav
		%ul
			%li
				%a{ :href => "/" } Index
			%li
				%a{ :href => "/information" } Information
			- external_links.each do |link|
				%li
					%a{ :href => "#{link[:service_url]}/#{link[:username]}" }= link[:service]